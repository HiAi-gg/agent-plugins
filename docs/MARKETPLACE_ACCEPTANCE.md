# Marketplace Acceptance

Acceptance evidence for the three generated client marketplace manifests, the
exact commands used, and the honest per-client status. Anything not actually
run in this environment is labeled and not claimed as tested.

## What is being accepted

| Manifest                           | Client(s)                   | Source shape                        | Plugins |
| ---------------------------------- | --------------------------- | ----------------------------------- | ------- |
| `.agents/plugins/marketplace.json` | OpenAI Codex / ChatGPT      | `git-subdir` (`url`, `path`, `ref`) | 13      |
| `.cursor-plugin/marketplace.json`  | Cursor                      | relative `./plugins/<name>`         | 13      |
| `.github/plugin/marketplace.json`  | GitHub Copilot CLI, VS Code | relative `./plugins/<name>`         | 13      |

All three are generated by `scripts/generate-marketplaces.ts` from the
canonical `plugins/<name>/plugin.json` files and must remain byte-identical to
a fresh generation (CI-enforced). Do not hand-edit them.

## Required validation matrix

| #   | Check                                                                                         | Command                                                                          | Result                                                                                                              |
| --- | --------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------- |
| 1   | Deterministic generation (run 1)                                                              | `bun run scripts/generate-marketplaces.ts --check`                               | PASS — zero diff                                                                                                    |
| 2   | Deterministic generation (run 2, repeat)                                                      | `bun run scripts/generate-marketplaces.ts --check`                               | PASS — zero diff                                                                                                    |
| 3   | JSON parse + semantic validation (13 names, versions, descriptions, source paths, no secrets) | `bun run scripts/validate-marketplaces.ts`                                       | PASS — "13 plugins x 3 manifests, sources, no secrets"                                                              |
| 4   | Builder 0.0.9 regeneration (all 13 plugins unchanged)                                         | `./scripts/repro-check.sh`                                                       | PASS — "all 13 clean"                                                                                               |
| 5   | Doctor 0.0.6 on all 13 plugins                                                                | `bunx @hiai-gg/agent-plugins-doctor@0.0.6 check plugins/<name>` (x13)            | PASS — 0 fixes needed, all 13                                                                                       |
| 6   | Official 1.0.0 schema validator, all 13                                                       | `python3 scripts/validate_plugin.py plugins/<name>` (x13)                        | PASS — 13/13                                                                                                        |
| 7   | Skill frontmatter, all skills                                                                 | covered by #6 (50 skills)                                                        | PASS — 50/50                                                                                                        |
| 8   | Secret scan (plugins + new manifests/scripts)                                                 | repo CI gate + grep over `.agents/ .cursor-plugin/ .github/plugin/ scripts/*.ts` | PASS — no matches                                                                                                   |
| 9   | CI workflow YAML validity                                                                     | `python3 -c "import yaml; yaml.safe_load(...)"` on both workflows                | PASS — valid YAML                                                                                                   |
| 10  | No client forks / duplicated plugin trees                                                     | `git status` + diff inspection (see below)                                       | PASS — only `marketplace.json` files added under `.agents/`, `.cursor-plugin/`, `.github/plugin/`; no plugin copies |
| 11  | Codex live clean-environment test (public GitHub main)                                        | see Codex section below                                                          | PASS — add exit 0, 13 plugins @ 0.0.2, 5 installs                                                                   |
| 12  | VS Code live UI test                                                                          | not possible — no `code` binary in environment                                   | NOT TESTED (documented, docs-verified)                                                                              |

## Codex — live CLI test (clean CODEX_HOME, public GitHub main)

Environment: `codex-cli 0.146.0`, `CODEX_HOME=/tmp/opencode/codex-clean`
(created empty). All output captured verbatim from this machine. Runs
post-push against the public repository `HiAi-gg/agent-plugins` `main`
(`bfc5ff8` and later).

### Attempt 1 — exact required command against the public repo

```bash
codex plugin marketplace add HiAi-gg/agent-plugins
```

Result: **PASS — exit 0** (clones the public `main`, which now contains
`.agents/plugins/marketplace.json`):

```
Added marketplace `hiai-agent-plugins` from https://github.com/HiAi-gg/agent-plugins.git
```

### Attempt 2 — marketplace discovery

```bash
codex plugin marketplace list
```

Result: **PASS** — `hiai-agent-plugins` registered.

### Attempt 3 — plugin catalog discovery (all 13 entries parsed)

```bash
codex plugin list --available --json
```

Result: **PASS** — exactly 13 plugins listed, all version `0.0.2` (github,
agent-browser, context7, firecrawl, redis, sentry, supabase, figma,
cloudflare, notion, docker, kubernetes, postgresql), each with the exact
`git-subdir` source
(`https://github.com/HiAi-gg/agent-plugins.git, path './plugins/<name>',
ref 'main'`).

### Attempt 4 — representative end-to-end installs

```bash
codex plugin add agent-browser@hiai-agent-plugins
codex plugin add context7@hiai-agent-plugins
codex plugin add github@hiai-agent-plugins
codex plugin add postgresql@hiai-agent-plugins
codex plugin add docker@hiai-agent-plugins
```

Result: **PASS — all 5 installed and enabled** (clone + sparse checkout of
`./plugins/<name>` from the real GitHub repo, version `0.0.2`):

```
Added plugin `agent-browser` from marketplace `hiai-agent-plugins`.
Installed plugin root: .../plugins/cache/hiai-agent-plugins/agent-browser/0.0.2
```

`codex plugin list` afterwards reports `installed, enabled 0.0.2` for all
five. The installed cache contained the skills directories where applicable
(3/3/4/5/4 across the five) and MCP metadata (`mcp.json` with `$schema` and
servers) for the plugins that ship it. Note: Codex materializes the whole
plugin directory (including authoring sources like `plugin.yml`/
`skills-src/`, which are harmless and produce only the informational DOC-5003
note if a client re-validates).

### Cleanup

The temporary `CODEX_HOME` was removed after verification; no temporary
state remains in this environment. All results above were produced from the
public `HiAi-gg/agent-plugins` repository `main` branch.

## VS Code

No `code` binary exists in this environment, so **no live UI test was run**.
Official, source-verified facts (documented, not claimed as tested):

- Install a plugin/marketplace repo: Command Palette →
  **`Chat: Install Plugin From Source`** (GitHub `owner/repo`, git URL, or
  local folder path).
- Git marketplaces: the `chat.plugins.marketplaces` setting accepts
  `owner/repo[#ref]`, git URIs, or `file://` URIs; VS Code clones each repo
  and looks for `marketplace.json` (OpenPlugin), `.plugin/marketplace.json`,
  **`.github/plugin/marketplace.json` (Copilot format — used by this repo)**,
  or `.claude-plugin/marketplace.json`.
- Trust: first install from a new marketplace shows a trust prompt.
- Docs: https://code.visualstudio.com/docs/agent-customization/agent-plugins
- Source: microsoft/vscode `src/vs/workbench/contrib/chat/common/plugins/pluginMarketplaceService.ts`

## Cursor, GitHub Copilot

No Cursor app and no `copilot` CLI binary exist in this environment; both are
**DOCS VERIFIED** (official docs + official example marketplaces). See
docs/CLIENT_DISTRIBUTION_MATRIX.md for the exact URLs and shapes. The
manifests follow the documented schemas verbatim:
`.cursor-plugin/marketplace.json` (relative sources) and
`.github/plugin/marketplace.json` (relative sources, the documented discovery
path).

## Kiro

No Git marketplace mechanism exists (documented in
docs/CLIENT_DISTRIBUTION_MATRIX.md). Kiro consumes individual Powers via
"Import power from GitHub" / "Import power from a folder" or the curated
`kiro.dev/powers` directory. This collection does not fabricate Kiro
marketplace support.

## No forks / no duplicated plugin trees

The diff adds exactly three `marketplace.json` files (one per client
directory) plus scripts/docs/CI. No plugin directories are copied or forked
under `.agents/`, `.cursor-plugin/`, or `.github/plugin/`. `git status` shows
no new plugin trees.
