# BUILDER_PROVENANCE — docker

| Field | Value |
|---|---|
| Builder version | @hiai-gg/agent-plugin-builder **0.0.6** |
| Builder command | `bunx @hiai-gg/agent-plugin-builder create --config plugin.yml --output plugins/docker` |
| Config file | `plugin.yml` (declarative source of truth) |
| Skill source files | `skills-src/docker/*.md` (bodies via `body-file`) |

## Files generated by Builder

- `plugin.json` — full manifest (version 0.0.1)
- `skills/inspect-docker-environment/SKILL.md`
- `skills/diagnose-container/SKILL.md`
- `skills/diagnose-compose/SKILL.md`
- `skills/review-dockerfile/SKILL.md`
- `README.md` (scaffold)

No `mcp.json` — intentionally skills-only.

## Manual edits after generation

| File | Edit | Type |
|---|---|---|
| `README.md` | Replaced scaffold with full public README | Content |
| `LICENSE` | Created (MIT, HiAI) | Content |
| `CHANGELOG.md` | Created (0.0.1) | Content |
| `docs/BUILD_NOTES.md`, `docs/UPSTREAM_TRUST.md`, `docs/BUILDER_PROVENANCE.md` | Created | Content |

Skill renames (`review-docker-environment` → `inspect-docker-environment`) and
the new `review-dockerfile` were done in the skill source files before
generation.

**Manual structural edits: NONE** — generated structure used as-is.

## Builder issues encountered

1. `create --config` ignores `--force` on non-empty output (directory must be
   removed first).
2. Generated README is a minimal scaffold.
3. No LICENSE / CHANGELOG generated.
